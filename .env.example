PORT=3000
ADMIN_PASSWORD=replace-with-a-long-unique-admin-password
SESSION_SECRET=replace-with-a-random-secret-at-least-32-characters-long
COOKIE_SECURE=false

# Set this to the mounted persistent volume path in production.
STATE_DIR=/data
