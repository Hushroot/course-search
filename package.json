{
  "name": "secure-course-search",
  "version": "1.3.0",
  "private": true,
  "description": "Private course CSV search with access codes and an admin panel.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/assets/login.js && node --check public/assets/app.js && node --check public/assets/admin.js && node --check scripts/setup.js",
    "setup": "node scripts/setup.js"
  },
  "engines": {
    "node": ">=20"
  }
}
